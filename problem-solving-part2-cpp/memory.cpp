
    #include <bits/stdc++.h>
    using namespace std;

    int main()
    {
        
        /*  memory -> has 2 parts
            1. stack memory -> compile time
            2. Heap / dynamic memory -> runtime

            cons of stack memeory
                -> can't allocate new memory on runtime
                -> can't return array from a function ( as array return address not value )
        */

       
       /* Create data in dynamic/heap memeory ( new )

        int *a = new int; // address created in heap memeory
        cout <<a <<endl; // get address
        *a = 20; // set value
        cout <<*a; // dereference the value

       */


        return 0;
    }